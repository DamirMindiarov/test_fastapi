name: FastApi CD

on:
  push:
    branches:
      - 'main'

jobs:
  run_tests:
    runs-on: ubuntu-latest

    steps:
      - name: TEST push
        run: echo 132

  deploy:
    needs: [ run_tests ]
    runs-on: ubuntu-latest

    steps:
      - name: Test pull request
        run: echo 456